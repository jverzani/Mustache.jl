language: julia
os:
    - linux
    - osx
julia:
    - 1.0
    - 1.1
    - nightly
notifications:
    email: false
sudo: false

after_success:
    - julia -e 'import Pkg; Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
